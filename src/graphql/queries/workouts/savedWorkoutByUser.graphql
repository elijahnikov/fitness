query Workouts($type:String,$limit: Int, $title: String){
  workouts(type:$type, limit:$limit, title:$title){
    workouts{
      id
      title
      type
      sets
      reps
      weight
      duration
      createdAt
      updatedAt
    }
  }
}