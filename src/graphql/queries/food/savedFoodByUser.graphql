query Foods($type: String,$limit: Int, $title: String){
  foods(limit:$limit, type:$type, title:$title){
    foods{
      id
      title
      serving
      calories
      protein
      carbs
      fat
      type
      pictureUrl
      createdAt
      updatedAt
    }
  }
}